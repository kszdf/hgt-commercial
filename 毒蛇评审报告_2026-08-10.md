# 毒蛇评审报告：慧根堂数字人短视频平台（独立第三方视角）

> 评审对象：hgt-commercial（Laravel 11 + Livewire/Flux 前端 + 8500 Python 出片微服务 + HEYGEM GPU 渲染）
> 评审日期：2026-08-10
> 评审立场：**独立第三方、非建设方、非使用方**。以下结论基于源码静态走查（非部署态压测），凡未实测处均标注"待核实"。
> 一句话定调：一个功能闭环已经跑通、对财税垂直场景的理解相当到位的产品；但在**架构可用性、并发一致性、前端数据传递、隐私泄露**这几件事上，带着"能跑就行"的作坊思维，离真正商用 SaaS 还差一口气。

---

## 1. 架构与稳定性（致命级）

### 1.1 整套生产环境跑在你的个人电脑里
- **现象**：Web（Laravel + nginx）、业务数据库（MySQL in Docker）、任务编排、GPU 渲染（HEYGEM）全部在本机 Windows + Docker Desktop 上，经 frp 隧道反代到公网 `zmgen.cn`。`com.docker.service` 一停，云端全 502（本次评审前已实证）。
- **根因**：既定决策是"HEYGEM 必须 GPU+Windows，所以全栈留本机"。但**这个理由只绑住了渲染 worker，绑不住 Web/API/数据库**。
- **影响**：你关机、断网、Docker 抽风、Windows 更新重启 = 全平台不可用，且是不可预测的不可用。商用 SaaS 把可用性押在个人电脑的开机状态上，是阿喀琉斯之踵。
- **建议（P0）**：把 Web + API + MySQL + 任务队列**上云**（你已有腾讯云固定 IP 机 `124.222.33.233`），本地只保留 GPU 渲染 worker，经消息队列/HTTP 受云调度。渲染不出本机可以，但"门户"必须脱离个人电脑。这样你关机不影响用户访问，GPU 挂了只影响出片排队。
- **工作量**：中（需拆分部署、引入队列或 HTTP 调度）。

### 1.2 数据无异地备份，本机硬盘是单点
- **现象**：用户视频产物、MySQL 数据全在本地磁盘。GitHub 每日自动推送的是**代码**，不是数据库，也不是视频文件。
- **影响**：本机硬盘损坏/误删 = 所有租户的视频库 + 账号 + 订单全部归零，且不可逆。
- **建议（P1）**：至少每日 `mysqldump` 加密上传到云对象存储（COS/OSS）；视频产物落地即同步一份到云存储。这是商用平台的最低生存线。
- **工作量**：小。

### 1.3 单 GPU 物理串行，"并发 3 路"是逻辑承诺
- **现象**：`GLOBAL_MAX_JOBS=3`（`server.py:699`）是内存计数，但 HEYGEM 渲染是 GPU 密集，3 个线程同时跑会抢同一张卡，实际是串行化 + 上下文切换。
- **影响**：峰值真实吞吐 < 3 路；"并发上限 3"给用户一种能并行 3 条的错觉，实际第 2、3 条在排队等卡。
- **建议（P2）**：要么明确对用户的 SLA 按"串行排队"措辞（不要承诺并行），要么上多卡/多机 worker 池。当前 queue-estimate 算法（`ceil(queued/3)*10`）按 3 路并行算等待，会**系统性低估**真实等待——这是个会误导用户的 bug。
- **工作量**：小（改算法口径）/ 大（加卡）。

---

## 2. 并发控制设计矛盾（逻辑漏洞）

### 2.1 双层闸口径不一，没有单一事实源
- **现象**：
  - Laravel 侧 `VideoController::generate()` 判 429 数的是**本租户 `status='queued'` 的库行数 ≥ 2**（含"在排队还没渲染的"+"正在渲染的"+"可能没被 video-sync 及时回写的陈旧行"）。
  - 8500 侧 `_handle_generate`（`server.py:1630`）判 429 数的是**进程内存里 `rendering` 的线程数**。
- **根因**：两个独立计数，对象不同、生命周期不同（Laravel 数库、8500 数内存），没有谁听谁的。
- **影响**：
  - Laravel 提前拦：用户提交第 3 条时，Laravel 看到 queued=2 就 429，但 8500 实际可能只有 1 个在渲染（另 1 个在排队），GPU 没吃饱就被前端拒了 → **利用率被人为压低**。
  - 反过来：8500 进程重启，`jobs` 字典清空，内存计数归零，但 Laravel 库里 queued 行还在（要等 video-sync 30s 兜底回写 failed），这期间 Laravel 会"虚高"地拒绝新提交。
- **建议（P0）**：选一个权威源。最干净的做法是**撤销 Laravel 侧的 queued 计数闸，只保留 8500 侧的 rendering 闸**（因为真正占用 GPU 的是 8500），Laravel 的 429 改为"查询 8500 实时并发后下发"。或者反过来，8500 完全信任 Laravel 下发的许可令牌。现在两套各算各的，迟早出诡异的"明明能提交却 429 / 明明满了却放行"。
- **工作量**：小。

### 2.2 rerender 不重置 start_ts，重渲染会被硬超时误杀
- **现象**：`run_job` 在 `server.py:1122` 设 `start_ts`，进入 rerender 分支（`server.py:1135`）时**只改 step 不改 start_ts**；watchdog（`server.py:853`）用 `now - start_ts > HARD_TIMEOUT+120` 判定僵尸。
- **影响**：若首渲染用了 25 分钟（接近 2100s 硬上限），触发 QC 不合格重渲染，重渲染再用 25 分钟，watchdog 在 35 分钟整（2220s）处把还在 rerender 的任务**一刀切掉**，用户拿到"渲染超时"失败——其实它快成功了。
- **建议（P1）**：rerender 分支重置 `start_ts = time.time()`（或给 rerender 单独计时配额）。
- **工作量**：极小。

### 2.3 8500 内存态 jobs，重启即丢在途任务
- **现象**：`jobs = {}`（`server.py:704`）是进程内存字典，重启 8500（你刚重启过一次）全部 in-flight 任务消失。
- **影响**：依赖 video-sync 守护进程查 8500 返回 404 → 标 failed 释放。但存在窗口：用户任务"正在渲染"期间你重启了 8500，前端轮询看到的是 404/失败，**用户以为出片失败，其实只是服务被重启**。
- **建议（P2）**：job 元数据已落盘（`_save_job` 写 `job.json`），重启时应从磁盘恢复 in-flight 任务而非直接判死；或至少前端对 404 给出"服务重启中，任务已重新排队"而非"失败"。
- **工作量**：中。

---

## 3. 前端工程与数据传递（实用性硬伤）

### 3.1 整篇口播稿被塞进 URL —— 商业脚本泄露到服务器日志（高危）
- **现象**：`rewrite.blade.php:1002 / 1012` 跳转出片页用
  `window.location.href = '/studio/scroll?...&dialogue=' + encodeURIComponent(r.data.cleaned)`
  把**整篇改写稿（可能几千字）拼进 URL 的 GET 参数**。
- **影响**：
  1. **隐私/商业机密泄露**：nginx `access_log` 默认记录完整 `$request`（含 URL），所有用户的口播脚本明文写进云端 + 本机 nginx 日志，且进入浏览器历史。
  2. **URL 长度风险**：超长稿可能触发浏览器/代理 URL 截断，跳转后文稿残缺。
  3. **冗余**：同一份 `cleaned` 又存了 `sessionStorage('hgt_rewrite_cleaned')`，URL 这一份纯属多余且有害。
- **建议（P0）**：跳转改为 POST 或仅传 `job_id`/草稿 ID，正文走 sessionStorage 或后端暂存；严禁把用户文稿放进 URL 查询串。这是当前最该立刻修的安全问题。
- **工作量**：极小。

### 3.2 跨页工作流靠 sessionStorage/localStorage，刷新/换设备即丢
- **现象**：选题→二创→出片跨页传递全靠 `sessionStorage`（`hgt_topic_*`、`hgt_rewrite_*`）和 URL 参数。
- **影响**：用户浏览器刷新、关页重开、换设备，草稿/选题上下文全失，得从头来。没有后端"我的草稿"概念。
- **建议（P1）**：选题、改写稿、出片配置都应落库（现有 `VideoJob`/`VideoJobBatch` 可扩展草稿态），用户在任何设备登录都能续接。
- **工作量**：中。

### 3.3 用户看不到"我的任务到底在渲染还是排队"
- **现象**：Laravel `VideoJob` 只有 `queued/done/failed`，本地永远显示 queued 直到完成。queue-estimate 给了全局排队预估，但**不给"你这一条现在什么状态"。
- **建议（P2）**：前端轮询时区分"已提交-排队中"与"渲染中"（可由 8500 返回 step 字段），让用户知道在等还是在做。
- **工作量**：小。

### 3.4 角色前缀清洗正则漏了"幕后音"（边界漏点）
- **现象**：`scroll.blade.php:577` 的 `stripRolePrefix` 正则含 `旁白|解说|主播|画外音|独白|配音`，但**刚统一的"幕后音"没加进去**。若 AI 输出"幕后音：xxx"前缀，不会被剥离，会原样带进配音文本。
- **影响**：统一叫法的收尾漏洞——前端清洗没跟上后端改名。
- **建议（P1）**：正则补 `幕后音`，并与 server.py 的角色指令保持同一份"前缀白名单"常量，避免再分裂。
- **工作量**：极小。

### 3.5 前端大量内联原生 JS，无组件化
- **现象**：`scroll.blade.php`、`rewrite.blade.php` 都是数百上千行原生 JS 内联，重复逻辑（标题建议、格式检测、轮询）散落多页。
- **影响**：维护成本高、易引入不一致（如 3.4 的漏改）。
- **建议（P2）**：核心交互抽成可复用 JS 模块/Alpine 组件；与 Flux/Livewire 技术栈对齐。
- **工作量**：大（渐进重构）。

---

## 4. 功能重叠与命名（可用性）

### 4.1 "质检(QC)"与"审核(Review)"对非技术用户是黑话
- **现象**：`studio/qc`（技术质检：嘴型/字幕/旧字幕残留）与 `studio/review`（人工过审）是两个独立页面。
- **影响**：B 端老板/运营分不清"质检"和"审核"谁先谁后、要不要都点。
- **建议（P2）**：合并为单一"出片前检查"流程，或明确命名为"AI 自动检查"+"人工确认发布"，用大白话。
- **工作量**：小。

### 4.2 rewrite 与 rewrite-original 两页高度重复
- **现象**：一个基于选题改写、一个空白稿改写，UI 与逻辑 90% 相同。
- **建议（P2）**：合并为一个改写页，用"从选题带入 / 空白开始"两种入口参数区分。
- **工作量**：中。

---

## 5. 可解释性与可操作性（输出质量）

### 5.1 错误信息技术化，对 B 端老板不可操作
- **现象**：8500 返回中文 error 字符串（如"渲染超时…请缩短内容或分批生成"），前端直接 `eb.textContent = '出片失败：' + data.error` 显示（`scroll.blade:946`）。
- **影响**：对财税老板用户，看到"HEYGEM 抽帧失败"或"剪辑后处理失败"依然一头雾水，不知道下一步点哪。
- **建议（P1）**：错误分级 + 操作指引。失败原因映射到"你可以做什么"（重试 / 缩短文稿 / 换模特 / 联系客服），而不是把后端异常原文甩给用户。
- **工作量**：小。

### 5.2 改写结果缺"为什么这么改"的解释
- **现象**：二创稿直接给 cleaned 文本，没有改动机说明（哪些违禁词被替换、为什么拆三段）。
- **建议（P2）**：附简短"改写说明"（命中 X 个违禁词已替换、已按开头/正文/结尾三段结构），提升可信度与可编辑性。
- **工作量**：小。

---

## 6. 安全与合规（已知 + 待查）

- **6.1 超管加固已完成**（前次会话将硬编码 `admin888` 改为 `.env` 强密码、tenant_id=null 真超管），此项不毒舌。
- **6.2 文件上传有校验**：`ModelAssetController` 模特上传 `mimes:mp4,mov,webm,max:204800`（200MB），有类型和大小限制，OK。
- **6.3 注册即建租户 + 7 天试用**：需确认试用硬限制（单条≤10 分钟、禁批量外发）在代码层是否真拦死，有无绕过路径（待核实）。
- **6.4 URL 泄露（见 3.1）**：当前最实质的安全问题，优先级高于上面所有"锦上添花"。

---

## 7. 优先级改进清单（P0→P2）

| 级别 | 问题 | 根因 | 建议 | 工作量 |
|---|---|---|---|---|
| **P0** | 整篇文稿进 URL，泄露到 nginx 日志 | 跳转用 GET 拼 dialogue | 改 POST 或只传草稿 ID，正文走 sessionStorage/后端 | 极小 |
| **P0** | 双层并发闸口径不一 | Laravel 数 queued / 8500 数 memory rendering，无单一源 | 撤销 Laravel 侧闸，只信 8500 实时并发 | 小 |
| **P0** | 生产全栈绑个人电脑 | 决策把 Web 也留本地 | Web/API/DB 上云，本地仅留 GPU worker | 中 |
| **P1** | 数据无异地备份 | 只备份代码 | 每日 mysqldump + 视频产物同步云存储 | 小 |
| **P1** | rerender 吃首次超时配额 | start_ts 未重置 | rerender 分支重置 start_ts | 极小 |
| **P1** | 跨页草稿丢 | 只靠 sessionStorage | 草稿落库，多端续接 | 中 |
| **P1** | 错误不可操作 | 后端异常原文直甩 | 错误分级 + 操作指引 | 小 |
| **P1** | 清洗正则漏"幕后音" | 改名不同步 | 补前缀白名单并统一常量 | 极小 |
| **P2** | 等待预估按 3 路并行，系统性低估 | 算法口径错 | 改为串行排队口径 | 小 |
| **P2** | 8500 重启丢在途任务 | jobs 内存态 | 磁盘恢复或前端友好提示 | 中 |
| **P2** | QC/Review 命名黑话 | 两页分离 | 合并或白话命名 | 小 |
| **P2** | rewrite 两页重复 | 入口不同 | 合并单页双入口 | 中 |
| **P2** | 前端无组件化 | 内联 JS | 抽 Alpine 组件 | 大 |

---

## 8. 结语（毒舌但公道）

平台在"财税垂直场景的理解"和"端到端把数字人出片跑通"这两件事上，做得比大多数同类 Demo 强——选题、违禁词、二创、配音、出片、质检、审核、发布一条龙，且针对金税四期老板痛点做了呈现形式细分，这是真懂行。

但**从"能演示"到"能商用"之间，差的不是功能多少，而是这几件"脏活"**：把门户搬离个人电脑、把数据库备份、把并发控制收敛成单一事实源、把用户文稿从 URL 里抠出来。这些事不性感、不出现在截图里，但每一件都在悄悄决定"用户会不会在某天早上打开发现全 502、或者发现自己的脚本躺在别人日志里"。

先修 P0 三件，尤其 URL 泄露和并发闸——这两个是现在就能捅娄子的。
