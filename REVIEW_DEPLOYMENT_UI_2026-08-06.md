# 追梦平台全面审查报告

> 审查时间：2026-08-06 18:40  
> 审查范围：本地→云端部署一致性、功能菜单逻辑、页面布局与 UI 细节  
> 当前关键状态：`zmgen.cn:8080` 已通（302→追梦登录页），`zmgen.cn:8500` 已通（404=出片服务健康），frpc 已常驻为 Windows 服务。

---

## 一、部署现状摘要

| 项目 | 状态 |
|------|------|
| 本地 Laravel（127.0.0.1:8080） | ✅ 302 健康 |
| 本地出片服务（127.0.0.1:8500） | ✅ 404 健康 |
| 云端 frps（systemd） | ✅ active，监听 7000/7500/8080/8500 |
| 本机 frpc（NSSM 服务） | ✅ Running/Automatic |
| 云端 UFW 防火墙 | ✅ 已放行 22/443/7000/8080/8500 |
| 域名 zmgen.cn:8080 | ✅ 通 |
| 域名 zmgen.cn:8500 | ✅ 通 |
| HTTPS | ❌ 未配置 |
| 云端 /opt standby | ⚠️ .env APP_URL 仍是 sjk.hgtcs.com:8080；有未提交修改；缺 nginx 容器 |

---

## 二、P0 级问题（影响可用性、稳定性或合规）

### 1. 工作流面板点击失效（严重功能缺陷）
- **位置**：`resources/views/dashboard.blade.php` 第 166-224 行
- **现象**：9 宫格工作流面板每个步骤都绑了 `click` 事件，内部执行 `e.preventDefault(); e.stopPropagation();`，导致点击后**不会跳转**，只弹出 tooltip。
- **风险**：用户登录后首页的快捷入口全部失灵，必须走左侧导航，严重违反直觉。
- **建议**：去掉 `e.preventDefault()`，让 `<a>` 正常跳转；tooltip 改为 hover 触发或独立信息图标。

### 2. 视频出片页默认文案违反"通用行业"铁律
- **位置**：`resources/views/studio/scroll.blade.php` 第 29-33 行
- **现象**：`textarea` 的 placeholder 和默认值是财税口播稿（"暂估成本""税务局"）。
- **风险**：直接违反 2026-08-02 用户拍板的"通用行业平台铁律"——代码/文案均不能限定财税。
- **建议**：换成中性示例，如"老板们注意了，合同条款里这个坑千万别踩……"或纯占位提示。

### 3. 试用版"批量外发"菜单无前置锁定标识
- **位置**：`resources/views/components/workspace-layout.blade.php` 第 57-63 行；`resources/views/studio/publish.blade.php` 第 12-17 行
- **现象**：免费套餐用户点击左侧"批量外发"，进入页面后才看到升级提示；菜单本身没有"试用不可用"或锁形图标。
- **风险**：用户被诱导进入后失望，体验差。
- **建议**：侧栏对试用不可用的菜单项加锁标/badge，或灰显；页面提示前置到 dashboard 状态条。

### 4. 192.168.4.103 为 DHCP，路由器重启会断隧道
- **位置**：`D:/frp/.../frpc.toml`
- **现象**：frpc 服务以 SYSTEM 账户运行，localIP 改为当前 DHCP 地址 `192.168.4.103`。
- **风险**：路由器重新分配 IP 或切换网络后，frpc 访问不到本机 Docker，zmgen.cn 直接 502。
- **建议**：在路由器 DHCP 里给本机 MAC 绑定固定 IP；或改用 `host.docker.internal` 不可行时，配置 Windows 路由/防火墙让 SYSTEM 能访问 127.0.0.1。

### 5. 无 HTTPS，浏览器标记"不安全"
- **位置**：域名 `zmgen.cn`
- **现象**：当前只通 `http://zmgen.cn:8080`，`https://zmgen.cn` 连接失败。
- **风险**：用户登录页输入密码被浏览器提示"不安全"，严重降低信任；存在中间人风险。
- **建议**：腾讯云申请免费 SSL 证书，配置 nginx 443 反向代理到 8080；最终 URL 改为 `https://zmgen.cn`。

### 6. 云端 standby 配置与主域名不一致
- **位置**：云端 `/opt/hgt-commercial/.env`
- **现象**：`APP_URL=http://sjk.hgtcs.com:8080`，与当前主域名 `zmgen.cn:8080` 不一致；且 git status 显示有未提交修改。
- **风险**：若未来切换路线 A 或恢复云端直出，链接/邮件/二维码会指向错误域名；standby 与 main 不同步。
- **建议**：云端 standby 强制同步 main 最新提交；.env APP_URL 改为 `http://zmgen.cn:8080`。

---

## 三、P1 级问题（影响体验、逻辑或维护）

### 7. 侧栏菜单分组与顺序不合理
- **位置**：`resources/views/components/workspace-layout.blade.php`
- **现象**："工作台总览"被放在"系统"分组最底部；生产管线 9 步顺序与 dashboard Hero 卡片区不一致。
- **建议**：工作台总览置顶（独立分组或放最上）；侧栏生产管线顺序与 01-09 编号严格对应。

### 8. 素材管理与生产管线割裂
- **位置**：侧栏"素材管理"分组 + 视频出片页
- **现象**：用户做视频时要选模特/声音/封面，但这些素材的上传管理在侧栏另一个分组；虽然出片页有快捷链接，但新用户会困惑。
- **建议**：在"视频出片"页内增加明显的"先去上传模特/声音/封面"的空状态引导；或把"素材管理"折叠到"视频出片"的二级菜单。

### 9. 数据复盘与数据录入合并
- **位置**：`workspace-layout.blade.php` 第 65-69 行
- **现象**：`/studio/analytics` 和 `/studio/metrics` 共用侧栏一个"数据复盘"入口，高亮逻辑是 `analytics || metrics`。
- **风险**：metrics 是手动录入/CSV导入/平台授权，对运营很关键，但用户可能找不到。
- **建议**：拆分为"数据复盘"和"数据录入"两个菜单项，或"数据复盘"下加展开子菜单。

### 10. 根路径重定向策略不佳
- **位置**：`routes/web.php` 第 16-18 行
- **现象**：`/` 永远重定向到 `/login`，即使用户已登录。
- **建议**：已登录用户访问 `/` 应重定向到 `/dashboard`。

### 11. 视频号在批量外发中仍作为可选平台
- **位置**：`app/Http/Controllers/PublishController.php` 第 37-39 行
- **现象**：平台列表包含"视频号"，且 UI 提示"client_credential 模式填密钥即生效"。
- **风险**：2026-08-06 已定论视频号无公开发布 API，这个提示会让用户误解为配置好就能自动发。
- **建议**：视频号从自动分发平台列表移除，改为"视频号需手动发布"的说明卡片；发布历史里可手动录入发布状态。

### 12. 表单错误提示只显示第一条
- **位置**：`resources/views/auth/login.blade.php` 第 21-25 行；`register.blade.php` 第 21-25 行
- **现象**：`$errors->first()` 只展示第一条错误。
- **建议**：列出所有错误项（尤其注册页有 4 个字段），或在字段旁显示对应错误。

### 13. 色调/命名不一致
- **位置**：多个 Blade 文件
- **现象**：成功提示有的用 `green`（`bg-green-50`），有的用 `emerald`（`bg-emerald-50`）；"质检未过"标签用 `red-700`，其他失败用 `red-600`。
- **建议**：统一语义色体系，成功=emerald，警告=amber，错误=red，信息=blue。

### 14. 试用状态条文案"约 5 元"写死
- **位置**：`resources/views/dashboard.blade.php` 第 22 行；`admin/billing.blade.php` 第 21 行
- **现象**：文案硬编码"约 5 元"，但 quota 可由 env 调整，若改为 5 次或 20 次都会误导。
- **建议**：根据 quota 动态计算金额文案，或改为"约 {{ quota/2 }} 元"（按单次约 0.5 元估算）。

---

## 四、P2 级问题（优化项）

### 15. 字体大小层级过碎
- **现象**：侧栏 13px、页面标题 16-20px、表单标签 14px、步骤编号 11px、描述 12px，视觉节奏不够统一。
- **建议**：参考 DESIGN.md 的 Type Scale，统一为 12/14/16/20/24px 五级。

### 16. 按钮样式不统一
- **现象**：原生 `<button>`、Flux button、表单提交按钮的圆角/padding/hover 不完全一致。
- **建议**：统一用 Flux 组件或统一 CSS class。

### 17. 暗色模式切换可能无效
- **位置**：`workspace-layout.blade.php` 第 142-147 行；`login.blade.php` 第 12-17 行
- **现象**：有 `toggleTheme()` 按钮，但未在全局看到初始化脚本。
- **建议**：在 `app-layout` 或公共 JS 中实现 `toggleTheme`，并持久化到 `localStorage`。

### 18. 封面预设分类切换无状态记忆
- **位置**：`resources/views/studio/covers.blade.php` 第 47-81 行
- **现象**：点击分类 tab 用 JS 切换 display，但无 URL hash 或 localStorage 记忆，刷新后回到第一个分类。
- **建议**：使用 URL hash 或 query string 记住当前分类。

### 19. 缺少 loading/skeleton 状态
- **位置**：选题/二创/出片等异步提交页面
- **现象**：提交按钮点击后没有全局 loading 遮罩或禁用态，用户可能重复点击。
- **建议**：异步提交时禁用按钮并显示 spinner。

### 20. 忘记密码未实现
- **位置**：`resources/views/auth/login.blade.php` 第 36 行
- **现象**："忘记密码？"链接是 `#`。
- **建议**：实现密码重置流程，或先隐藏该链接。

---

## 五、优化优先级总览

| 优先级 | 数量 | 建议立即处理项 |
|--------|------|----------------|
| P0 | 6 | 工作流面板跳转、出片页财税文案、批量外发试用锁定、DHCP 固定 IP、HTTPS、云端 standby 同步 |
| P1 | 8 | 菜单顺序、素材管线打通、数据菜单拆分、根路径重定向、视频号移除、错误提示、色调统一、试用文案动态化 |
| P2 | 6 | 字体统一、按钮统一、暗色模式、封面 tab 记忆、loading 状态、忘记密码 |

---

## 六、下一步建议

1. **先修 P0**：工作流面板跳转和出片页财税文案是 5 分钟能改完但影响巨大的问题。
2. **部署加固**：路由器绑定本机固定 IP + 腾讯云配 HTTPS（可用免费证书）。
3. **云端同步**：把 `/opt/hgt-commercial` 对齐 main 并改 .env APP_URL。
4. **再处理 P1/P2**：按上表分批迭代，每次改完走 `scripts/build-and-verify.ps1` + 清缓存 + 重启容器。
