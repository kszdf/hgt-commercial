# 追梦短视频平台 · 严格客户体验审核报告

> 审核时间：2026-08-08｜审核人：Diana（设计系统架构师 / QA）
> 方法：登录态实测 13 个页面 + 11 个含 JS 视图语法体检 + 核心 API 端到端（含边界）+ 上传安全核查 + 跨页数据交接一致性核对

## 一、界面体验

| 项 | 结果 | 说明 |
|---|---|---|
| 页面渲染 | ✅ 通过 | 13 个 studio/管理页全部 HTTP 200，无 PHP 异常 / Whoops |
| JS 语法 | ✅ 通过 | 11 个含 `<script>` 视图全部 `node --check` 通过（历史引号错误已根除） |
| 加载 / 错误提示 | ✅ 通过 | 选题 / 二创 / 出片均有 loading 动画 + 按钮禁用 + 错误框；后端错误 JSON 有前端兜底解析（含 HTML 异常页提取） |
| 视觉一致性 | ⚠️ 已修 | 发布页「批量发布」按钮原用 `bg-indigo-600`，全站统一 `brand` 色系 → 已改为 `bg-brand-600` |

## 二、功能闭环（全通过）

创作主链路：**选题 →（sessionStorage）→ 二创 →（sessionStorage）→ 出片 → 人工审核 → 发布**

- **选题**：正常生成 / 选填全空 + count=1 / count=0·11 越界 422 —— 实测全部符合预期
- **二创**：单条「去二创」、批量「全部二创」、自由创作三模式齐备；「带稿去出片」交接验证通过
- **出片**：空文稿 422、超长时长超限 422、最小有效稿返回 `job_id`（8500 在线接单）；并发在阈值内放行
- **选题→二创映射**：选题 `form` 三取值（单声口播 / 幕后音口播_双人 / 幕后音口播_单人）与二创 `mapTopicFormToMode` **完全对齐**，无「单人口播变双声」类错位（上一轮问题已根除）
- **审核 / 发布**：发布页试用拦截 + OAuth 授权 + 发布历史完整；审核页按租户过滤；出片完成自动进入 `draft` 待审态

**结论：核心创作闭环真实打通，无断链、无死循环、无未处理异常。**

## 三、边界与异常（已验证）

| 场景 | 行为 | 判定 |
|---|---|---|
| 选题全空 + count=1 | 正常返回 1 条 | ✅ 选填兼容 |
| 出片空文稿 | 422 `dialogue field is required` | ✅ 拦截 |
| 出片超长（≈5333s） | 422 `duration_exceeded` | ✅ 时长硬约束 |
| count=0 / 11 | 422 `between 1 and 10` | ✅ 校验 |
| 同租户并发第 3 个任务 | 429 `tenant_busy`（阈值=2） | ✅ 并发护栏 |
| 登录 CSRF | 登录后 token 轮换，POST 全带 CSRF + 限流 5/min | ✅ 安全 |
| 网络抖动 | 出片轮询 `catch` 静默续轮；非 JSON 响应有兜底提示 | ✅ 容错 |

## 四、商用就绪性

| 维度 | 结果 | 说明 |
|---|---|---|
| 数据持久化 | ✅ | VideoJob / QcReport / 封面 / 模特 / 声音均落库，按 `tenant_id` 隔离 |
| 权限控制 | ✅ | `auth` 中间件 + 租户归属校验（qcVideo / review / publish）；试用到期 402、配额 402 |
| 安全 | ✅ | CSRF 全覆盖；登录/注册/找回限流；**上传 MIME+大小严格校验**（模特 mp4/mov/webm≤200MB、封面 image≤10MB + `getimagesize`、声音 wav/mp3…≤30MB + 性别必填） |
| 性能 | ✅ | gzip + 静态资源 `immutable` 缓存（前轮已开）；出片长任务异步 + 轮询不阻塞主线程 |
| 支付 / 邮件 / 监控 | ⚠️ 待配 | 代码已落地，密钥缺失时优雅降级；需用户填 `.env`（微信/支付宝/SES/Sentry）才真正生效 |

## 五、问题严重等级汇总

- **阻塞**：无
- **严重**：无
- **一般（已修）**：
  1. 发布页按钮颜色不一致（`indigo` → `brand`）—— 已修复并重启生效
- **一般（建议优化）**：
  2. 出片轮询硬上限 10 分钟；数字人（avatar）长任务可能显示「超时」（任务仍在跑，回来可续接）。建议上限提至 15–20 分钟或将文案改为「后台续接中」
  3. 内置数字人模特选项硬编码（「张老师」/ 固定文件名），建议改为数据库 /「我的模特」驱动，避免通用平台出现具体人名
  4. 上传接口已校验，生产环境建议补充：文件类型二次验证 / 病毒扫描；声音克隆建议加租户月度用量上限防滥用
- **提示**：本次实测在用户 GPU 机上创建了 2 个测试出片任务（`747d0b89…` / `ecf34a86…`），会在审核队列出现并占用少量配额，可忽略或删除

## 六、结论

平台已达**商用就绪**水平：核心创作闭环真实跑通、边界防护到位、安全与权限规范。剩余均为体验优化项（一般级），**无阻塞 / 严重缺陷**。建议：① 补充 `.env` 支付 / 邮件 / 监控密钥后即可正式商用；② 微调数字人长任务轮询上限。
